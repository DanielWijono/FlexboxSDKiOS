/*
 * Intentionally empty translation unit.
 *
 * SwiftPM requires at least one compilable source in a C target. All this
 * target contributes is the umbrella header in `include/` which re-exports
 * `<yoga/Yoga.h>` under the `CYoga` module name.
 */
